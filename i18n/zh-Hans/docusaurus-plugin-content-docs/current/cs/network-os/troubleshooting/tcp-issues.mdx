---
title: TCP 问题
description: 超时、重置和 TIME_WAIT 排查步骤
---

# TCP 问题

## 常见症状

- 连接超时
- 连接被重置
- 大量 `TIME_WAIT` 状态

## 排查步骤

1. 使用 `ss -tan` 检查 socket 状态
2. 使用 `nc -vz` 验证端口可达性
3. 如果行为间歇性，使用 `tcpdump` 抓包分析
