---
title: "8 多模态提示工程"
description: "GPT-4V、Claude 和 Gemini 的视觉-文本提示、文档理解、视频分析和跨模态推理"
published: 2025-01-21
tags: ["prompt-engineering", "multimodal", "vision", "ai", "document-understanding"]
category: "prompt-engineering"
series: "提示工程指南"
seriesOrder: 7
draft: false
---

## 简介

**多模态 AI** 代表了不同输入模态（文本、图像、音频、视频和文档）的融合，形成了统一的推理系统。现代视觉语言模型（VLM），如 GPT-4V、Claude 4 和 Gemini 2.0，能够理解复杂的视觉场景、从文档中提取信息、分析图表，并同时推理多个图像。

本章介绍多模态系统的实用提示工程技术，包含生产就绪的 Spring AI 实现。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    多模态 AI 景观 (2025)                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                         输入模态                                 │  │
│  ├──────────┬──────────┬──────────┬──────────┬──────────────────────┤  │
│  │  文本    │  图像    │  音频    │  视频    │  文档                │  │
│  │  ▼       │  ▼       │  ▼       │  ▼       │  ▼                   │  │
│  │  NLP     │  视觉    │  语音    │  时序    │  OCR + 布局          │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────────────────┘  │
│                              │                                          │
│                              ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    统一推理引擎                                   │  │
│  │                                                                  │
│  │   跨模态注意力 • 语义对齐 • 锚定                                │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                              │                                          │
│                              ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      输出能力                                     │  │
│  ├──────────┬──────────┬──────────┬──────────┬──────────────────────┤  │
│  │ 分析    │ 提取     │ 问答     │ 生成     │  操作                │  │
│  │ 报告    │ JSON/CSV │  响应    │  内容    │  工具调用            │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

:::info
**性能提示**: 当提示包含明确的架构定义和验证约束时，多模态模型在文档提取任务中可实现 90%+ 的准确率（Google Research, 2024）。
:::

---

## 1. 模型能力对比 (2025)

了解模型优势有助于为每个任务选择合适的工具：

### 能力矩阵

| 特性 | **GPT-4o** | **Claude 4** | **Gemini 2.0** |
|---------|------------|--------------|----------------|
| **图像输入** | ✅ 原生支持 | ✅ 原生支持 | ✅ 原生支持 |
| **多图像** | ✅ 最多 20 张 | ✅ 最多 20 张 | ✅ 最多 3600 张 |
| **PDF 处理** | ⚠️ 通过图像 | ✅ 原生支持 | ✅ 原生支持 |
| **视频输入** | ⚠️ 帧提取 | ❌ | ✅ 原生支持（1小时） |
| **音频输入** | ✅ 通过 Whisper | ⚠️ 单独服务 | ✅ 原生支持 |
| **图像生成** | ✅ DALL-E 3 | ❌ | ✅ Imagen 3 |
| **空间理解** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **OCR 准确率** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **图表分析** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **医学影像** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### 模型选择指南

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    多模态模型选择                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────────────┐                                              │
│   │ 您的主要输入类型是  │                                              │
│   │      什么？        │                                              │
│   └──────────┬──────────┘                                              │
│              │                                                          │
│   ┌──────────┼──────────┬──────────────┬───────────────┐               │
│   ▼          ▼          ▼              ▼               ▼               │
│  图像      文档        视频        音频         混合媒体              │
│   │          │          │              │               │               │
│   │          │          │              │               │               │
│   ▼          ▼          ▼              ▼               ▼               │
│  任何     Claude 4   Gemini 2.0   GPT-4o/      Gemini 2.0           │
│  模型     (PDF 原生) (原生)     Gemini     (最佳跨模态)              │
│           GPT-4o                                (支持多模态)         │
│           Gemini                                                     │
│                                                                         │
│   对于 OCR/提取：Claude 4 > Gemini > GPT-4o                          │
│   对于创意任务：GPT-4o（带 DALL-E）> Gemini > Claude                │
│   对于长上下文：Gemini（200 万 token）> Claude（20 万）> GPT（128K） │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 视觉-文本提示基础

### 2.1 基础视觉提示结构

有效视觉提示的关键是提供关于图像内容的清晰上下文以及预期的分析类型：

```java
public class VisionPromptBuilder {

    /**
     * 标准视觉提示模板
     */
    private static final String VISION_TEMPLATE = """
        <context>
        这个图像是：{imageContext}
        </context>

        <task>
        {taskDescription}
        </task>

        <focus_areas>
        注意以下方面：
        {focusAreas}
        </focus_areas>

        <constraints>
        {constraints}
        </constraints>

        <output_format>
        {outputFormat}
        </output_format>
        """;

    public String buildPrompt(VisionPromptConfig config) {
        return VISION_TEMPLATE
            .replace("{imageContext}", config.imageContext())
            .replace("{taskDescription}", config.task())
            .replace("{focusAreas}", formatList(config.focusAreas()))
            .replace("{constraints}", formatList(config.constraints()))
            .replace("{outputFormat}", config.outputFormat());
    }

    public record VisionPromptConfig(
        String imageContext,
        String task,
        List<String> focusAreas,
        List<String> constraints,
        String outputFormat
    ) {}
}
```

### 2.2 图像分析模式

#### 模式 1：描述性分析

```java
String descriptivePrompt = """
    <task>
    全面描述此图像。
    </task>

    <structure>
    1. **概述**：主要主题/场景是什么？
    2. **细节**：描述具体元素、颜色、纹理
    3. **上下文**：描绘了什么环境或场景？
    4. **氛围**：图像传达了什么感觉？
    5. **技术方面**：构图、照明、风格
    </structure>

    <output_format>
    提供遵循上述结构的描述性内容。
    使用具体、明确的语言，而非模糊术语。
    </output_format>
    """;
```

#### 模式 2：分析性评估

```java
String analyticalPrompt = """
    <role>
    您是审查视觉内容的专家分析师。
    </role>

    <task>
    分析此图像的 {analysisType}。
    </task>

    <criteria>
    根据以下标准进行评估：
    1. {criterion1}: [定义]
    2. {criterion2}: [定义]
    3. {criterion3}: [定义]
    </criteria>

    <output_format>
    ## 分析总结
    [简短概述]

    ## 详细评估
    | 标准 | 评分（1-10） | 证据 | 建议 |
    |-----------|--------------|----------|-----------------|
    | ... | ... | ... | ... |

    ## 优先行动
    1. [最重要的建议]
    2. [第二优先级]
    3. [第三优先级]
    </output_format>
    """;
```

#### 模式 3：对比分析

```java
String comparativePrompt = """
    <task>
    比较 {count} 张图像并识别：
    1. 所有图像的相似之处
    2. 它们之间的关键差异
    3. 按 {criteria} 排序
    </task>

    <analysis_dimensions>
    - 视觉元素
    - 质量指标
    - 内容准确性
    - 风格一致性
    </analysis_dimensions>

    <output_format>
    ## 相似之处
    [共同元素]

    ## 差异
    | 方面 | 图像 1 | 图像 2 | 图像 3 |
    |--------|---------|---------|---------|
    | ... | ... | ... | ... |

    ## 排序
    1. [最佳] - 原因：...
    2. [第二] - 原因：...
    3. [第三] - 原因：...

    ## 建议
    [选择哪个以及原因]
    </output_format>
    """;
```

### 2.3 空间理解

现代 VLM 可以理解空间关系。使用明确的空间语言：

```java
String spatialPrompt = """
    <task>
    分析此图像的空间布局。
    </task>

    <focus>
    识别并描述：
    1. **位置**：每个元素在哪里？（左上、中心、右下等）
    2. **关系**：元素如何空间关联？
       - 上/下
       - 左/右
       - 前/后
       - 内/外
       - 相邻/远离
    3. **大小**：元素的相对大小
    4. **对齐**：元素是否对齐？沿什么轴？
    </focus>

    <output_format>
    ## 元素清单
    | 元素 | 位置 | 大小（相对） |
    |---------|----------|-----------------|
    | ... | ... | ... |

    ## 空间关系
    - [元素 A] 位于 [关系] [元素 B]
    - ...

    ## 布局评估
    [整体空间组织]
    </output_format>
    """;
```

---

## 3. 文档理解

文档理解是最有价值的多模态应用之一。它结合了 OCR、布局分析和语义提取。

### 3.1 文档类型分类

```java
@Service
public class DocumentClassificationService {

    private final ChatClient chatClient;

    public record DocumentClassification(
        String documentType,
        double confidence,
        List<String> extractableFields,
        String processingRecommendation
    ) {}

    private static final String CLASSIFICATION_PROMPT = """
        <task>
        将此文档图像分类为以下类别之一：
        </task>

        <categories>
        - invoice: 账单、发票、付款请求
        - receipt: 购收据、交易记录
        - form: 申请表、调查问卷
        - contract: 法律协议、服务条款
        - id_document: 身份证、护照、许可证
        - letter: 信函、官方信件
        - report: 业务报告、财务报表
        - other: 其他任何文档类型
        </categories>

        <output_format>
        仅返回 JSON：
        {
          "documentType": "invoice",
          "confidence": 0.95,
          "extractableFields": ["invoice_number", "date", "total", ...],
          "processingRecommendation": "使用发票提取模板"
        }
        </output_format>
        """;

    public DocumentClassification classify(byte[] documentImage, String mimeType) {
        UserMessage message = new UserMessage(
            CLASSIFICATION_PROMPT,
            new Media(mimeType, documentImage)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(DocumentClassification.class);
    }
}
```

### 3.2 发票/收据提取

```java
@Service
public class InvoiceExtractionService {

    private final ChatClient chatClient;

    public record Invoice(
        String invoiceNumber,
        LocalDate invoiceDate,
        LocalDate dueDate,
        Vendor vendor,
        Customer customer,
        List<LineItem> lineItems,
        TaxInfo taxInfo,
        MonetaryAmount total,
        String currency,
        String paymentTerms
    ) {}

    public record Vendor(
        String name,
        String address,
        String taxId,
        String email,
        String phone
    ) {}

    public record LineItem(
        int lineNumber,
        String description,
        String sku,
        BigDecimal quantity,
        String unit,
        BigDecimal unitPrice,
        BigDecimal lineTotal
    ) {}

    public record TaxInfo(
        BigDecimal subtotal,
        BigDecimal taxRate,
        BigDecimal taxAmount,
        String taxType
    ) {}

    private static final String INVOICE_EXTRACTION_PROMPT = """
        <role>
        您是专业的发票数据提取系统。
        高精度提取结构化数据。
        </role>

        <task>
        从此文档图像中提取所有发票信息。
        </task>

        <extraction_rules>
        1. 按原样提取精确文本（不要解释或转换）
        2. 对于日期，使用 ISO 格式：YYYY-MM-DD
        3. 对于金额，使用不包含货币符号的数值
        4. 如果字段不可见或不清楚，使用 null
        5. 对于行项目，提取所有可见项目
        6. 为地址保留原始格式
        </extraction_rules>

        <validation>
        - 行项目总计应匹配：数量 × 单价
        - 小计应匹配行项目总和
        - 总计应匹配：小计 + 税费
        - 在 validation_notes 中标记任何差异
        </validation>

        <output_format>
        返回与此架构匹配的有效 JSON：
        {
          "invoiceNumber": "string",
          "invoiceDate": "YYYY-MM-DD",
          "dueDate": "YYYY-MM-DD or null",
          "vendor": {
            "name": "string",
            "address": "string",
            "taxId": "string or null",
            "email": "string or null",
            "phone": "string or null"
          },
          "customer": {
            "name": "string",
            "address": "string"
          },
          "lineItems": [
            {
              "lineNumber": 1,
              "description": "string",
              "sku": "string or null",
              "quantity": number,
              "unit": "string or null",
              "unitPrice": number,
              "lineTotal": number
            }
          ],
          "taxInfo": {
            "subtotal": number,
            "taxRate": number (作为小数，例如 0.10 表示 10%),
            "taxAmount": number,
            "taxType": "VAT/GST/Sales Tax/etc"
          },
          "total": number,
          "currency": "USD/EUR/etc",
          "paymentTerms": "string or null",
          "validationNotes": ["发现的任何差异"]
        }
        </output_format>
        """;

    public Invoice extractInvoice(byte[] documentImage, String mimeType) {
        UserMessage message = new UserMessage(
            INVOICE_EXTRACTION_PROMPT,
            new Media(mimeType, documentImage)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(Invoice.class);
    }
}
```

### 3.3 表单提取与字段映射

```java
@Service
public class FormExtractionService {

    private final ChatClient chatClient;

    /**
     * 动态表单提取，支持可配置字段
     */
    public Map<String, Object> extractForm(
            byte[] formImage,
            String mimeType,
            FormSchema schema) {

        String prompt = buildFormExtractionPrompt(schema);

        UserMessage message = new UserMessage(prompt, new Media(mimeType, formImage));

        String response = chatClient.prompt()
            .user(message)
            .call()
            .content();

        return parseAndValidate(response, schema);
    }

    private String buildFormExtractionPrompt(FormSchema schema) {
        StringBuilder sb = new StringBuilder();

        sb.append("""
            <role>
            您是表单数据提取专家。
            </role>

            <task>
            从此表单图像中提取以下字段：
            </task>

            <fields>
            """);

        for (FieldDefinition field : schema.fields()) {
            sb.append(String.format("""
                - **%s** (%s)：
                  - 描述：%s
                  - 预期格式：%s
                  - 必需：%s
                  - 验证：%s

                """,
                field.name(),
                field.type(),
                field.description(),
                field.format(),
                field.required(),
                field.validation()
            ));
        }

        sb.append("""
            </fields>

            <instructions>
            1. 寻找与字段名称匹配或相似的标签
            2. 提取与每个标签关联的值
            3. 将复选框处理为布尔值（如果选中则为 true）
            4. 将多选处理为数组
            5. 对于手写文本，尽可能准确转录
            6. 如果对值不确定，包含置信度分数
            </instructions>

            <output_format>
            返回包含提取值的 JSON：
            {
              "fieldName": {
                "value": "提取的值",
                "confidence": 0.95,
                "rawText": "如果不同，则为原始文本"
              },
              ...
            }
            </output_format>
            """);

        return sb.toString();
    }

    public record FormSchema(
        String formName,
        List<FieldDefinition> fields
    ) {}

    public record FieldDefinition(
        String name,
        String type,
        String description,
        String format,
        boolean required,
        String validation
    ) {}
}
```

### 3.4 表格提取

```java
@Service
public class TableExtractionService {

    private final ChatClient chatClient;

    private static final String TABLE_EXTRACTION_PROMPT = """
        <task>
        将此图像中的表格提取为结构化格式。
        </task>

        <instructions>
        1. 识别图像中的所有表格
        2. 对于每个表格：
           - 提取标题（通常是第一行）
           - 提取所有数据行
           - 保留单元格对齐（左/中/右）
           - 通过重复值处理合并单元格
           - 将空单元格处理为 null
        3. 保持原始数据类型：
           - 数字作为数字
           - 日期使用 ISO 格式
           - 文本作为字符串
        </instructions>

        <output_format>
        返回 JSON：
        {
          "tables": [
            {
              "tableIndex": 1,
              "title": "如果可见的表格标题",
              "headers": ["列1", "列2", ...],
              "rows": [
                ["值1", "值2", ...],
                ["值1", "值2", ...]
              ],
              "metadata": {
                "rowCount": 数字,
                "columnCount": 数字,
                "hasHeaderRow": 布尔值
              }
            }
          ]
        }
        </output_format>
        """;

    public record TableExtractionResult(
        List<ExtractedTable> tables
    ) {}

    public record ExtractedTable(
        int tableIndex,
        String title,
        List<String> headers,
        List<List<Object>> rows,
        TableMetadata metadata
    ) {}

    public TableExtractionResult extractTables(byte[] image, String mimeType) {
        UserMessage message = new UserMessage(
            TABLE_EXTRACTION_PROMPT,
            new Media(mimeType, image)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(TableExtractionResult.class);
    }

    /**
     * 转换为 CSV 格式
     */
    public String tableToCsv(ExtractedTable table) {
        StringBuilder csv = new StringBuilder();

        // 标题
        csv.append(String.join(",", table.headers())).append("\n");

        // 行
        for (List<Object> row : table.rows()) {
            String rowStr = row.stream()
                .map(v -> v == null ? "" : escapeCSV(v.toString()))
                .collect(Collectors.joining(","));
            csv.append(rowStr).append("\n");
        }

        return csv.toString();
    }
}
```

---

## 4. 图表和数据可视化分析

### 4.1 图表理解

```java
@Service
public class ChartAnalysisService {

    private final ChatClient chatClient;

    public record ChartAnalysis(
        String chartType,
        String title,
        ChartData data,
        List<Insight> insights,
        List<String> limitations
    ) {}

    public record ChartData(
        String xAxisLabel,
        String yAxisLabel,
        List<DataSeries> series,
        Map<String, Object> extractedValues
    ) {}

    public record DataSeries(
        String name,
        List<DataPoint> points
    ) {}

    public record DataPoint(
        String label,
        Double value,
        Double confidence
    ) {}

    public record Insight(
        String type,  // 趋势、异常、比较、模式
        String description,
        String evidence,
        String significance
    ) {}

    private static final String CHART_ANALYSIS_PROMPT = """
        <role>
        您是数据可视化分析专家，擅长从图表中提取洞察。
        </role>

        <task>
        全面分析此图表/图像。
        </task>

        <analysis_steps>
        1. **图表识别**
           - 这是什么类型的图表？（柱状图、折线图、饼图、散点图等）
           - 它可视化什么内容？

        2. **数据提取**
           - 读取轴标签和刻度
           - 尽可能准确提取数据点
           - 记录任何图例或类别

        3. **趋势分析**
           - 识别整体趋势（上升、下降、稳定）
           - 记录任何拐点或方向变化

        4. **模式识别**
           - 识别季节性模式
           - 注意循环行为
           - 识别系列之间的相关性

        5. **异常检测**
           - 识别异常值或不寻常值
           - 记录任何数据缺口

        6. **关键洞察**
           - 主要要点是什么？
           - 数据讲述了什么故事？
        </analysis_steps>

        <output_format>
        返回结构化 JSON：
        {
          "chartType": "line/bar/pie/scatter/etc",
          "title": "图表标题（如果可见）",
          "data": {
            "xAxisLabel": "string",
            "yAxisLabel": "string",
            "series": [
              {
                "name": "系列名称",
                "points": [
                  {"label": "Jan", "value": 100, "confidence": 0.9}
                ]
              }
            ],
            "extractedValues": {
              "max": 数字,
              "min": 数字,
              "average": 数字
            }
          },
          "insights": [
            {
              "type": "trend",
              "description": "销售额同比增长 25%",
              "evidence": "线图显示持续上升趋势",
              "significance": "表明市场扩张"
            }
          ],
          "limitations": ["Y 轴刻度使小变化看起来很戏剧化"]
        }
        </output_format>
        """;

    public ChartAnalysis analyzeChart(byte[] chartImage, String mimeType) {
        UserMessage message = new UserMessage(
            CHART_ANALYSIS_PROMPT,
            new Media(mimeType, chartImage)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(ChartAnalysis.class);
    }

    /**
     * 从图表生成叙述性摘要
     */
    public String generateChartNarrative(ChartAnalysis analysis) {
        String prompt = """
            <task>
            为此图表分析生成专业的叙述性摘要。
            </task>

            <analysis>
            {analysis}
            </analysis>

            <style>
            - 使用清晰、专业的商业语言写作
            - 以最重要的洞察开头
            - 在可能的情况下使用具体数字
            - 承认任何限制或不确定性
            - 保持 2-3 段落长度
            </style>
            """;

        return chatClient.prompt()
            .user(u -> u.text(prompt)
                .param("analysis", toJson(analysis)))
            .call()
            .content();
    }
}
```

### 4.2 仪表板分析

```java
String dashboardPrompt = """
    <role>
    您是商业智能分析师，正在审查仪表板。
    </role>

    <task>
    分析此仪表板截图并提供高管洞察。
    </task>

    <analysis_framework>
    1. **KPI 概览**
       - 识别所有可见的关键绩效指标
       - 记录当前值和趋势（上升/下降箭头）

    2. **健康评估**
       - 哪些指标表现良好（绿色）？
       - 哪些需要关注（黄色/红色）？
       - 是否有任何关键警报？

    3. **关系**
       - 不同的指标如何关联？
       - 是否可见相关性？

    4. **建议行动**
       - 领导层应该关注什么？
       - 需要什么后续分析？
    </analysis_framework>

    <output_format>
    ## 执行摘要
    [一段概述]

    ## 关键指标状态
    | 指标 | 值 | 趋势 | 状态 |
    |--------|-------|-------|--------|
    | ... | ... | ↑/↓/→ | 🟢/🟡/🔴 |

    ## 关注领域
    1. [问题 + 建议行动]

    ## 积极指标
    1. [成功 + 有效的方法]

    ## 建议行动
    1. [优先行动项目]
    </output_format>
    """;
```

---

## 5. Spring AI 视觉集成

### 5.1 完整视觉服务

```java
@Service
public class VisionService {

    private final ChatClient chatClient;
    private final ImageProcessingService imageProcessor;

    public VisionService(ChatClient.Builder builder, ImageProcessingService imageProcessor) {
        this.chatClient = builder.build();
        this.imageProcessor = imageProcessor;
    }

    /**
     * 使用自定义提示分析单个图像
     */
    public String analyzeImage(byte[] imageData, String mimeType, String prompt) {
        // 如需要，预处理图像
        byte[] processedImage = imageProcessor.optimize(imageData, mimeType);

        UserMessage message = new UserMessage(
            prompt,
            new Media(mimeType, processedImage)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .content();
    }

    /**
     * 分析多个图像
     */
    public String analyzeMultipleImages(
            List<ImageInput> images,
            String prompt) {

        // 构建包含多个媒体的消息
        UserMessage.Builder messageBuilder = UserMessage.builder()
            .text(prompt);

        for (ImageInput img : images) {
            messageBuilder.media(img.mimeType(), img.data());
        }

        return chatClient.prompt()
            .user(messageBuilder.build())
            .call()
            .content();
    }

    /**
     * 从图像提取结构化数据
     */
    public <T> T extractStructured(
            byte[] imageData,
            String mimeType,
            String extractionPrompt,
            Class<T> targetClass) {

        UserMessage message = new UserMessage(
            extractionPrompt,
            new Media(mimeType, imageData)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(targetClass);
    }

    /**
     * 带对话上下文的视觉分析
     */
    public String analyzeWithContext(
            byte[] imageData,
            String mimeType,
            String question,
            List<Message> conversationHistory) {

        List<Message> messages = new ArrayList<>(conversationHistory);
        messages.add(new UserMessage(question, new Media(mimeType, imageData)));

        return chatClient.prompt()
            .messages(messages)
            .call()
            .content();
    }

    public record ImageInput(String mimeType, byte[] data, String description) {}
}
```

### 5.2 图像预处理服务

```java
@Service
public class ImageProcessingService {

    private static final int MAX_DIMENSION = 2048;
    private static final int TARGET_QUALITY = 85;
    private static final Set<String> SUPPORTED_TYPES = Set.of(
        "image/jpeg", "image/png", "image/gif", "image/webp"
    );

    /**
     * 优化图像以进行 API 传输
     */
    public byte[] optimize(byte[] imageData, String mimeType) {
        try {
            BufferedImage image = ImageIO.read(new ByteArrayInputStream(imageData));

            // 如果太大则调整大小
            if (image.getWidth() > MAX_DIMENSION || image.getHeight() > MAX_DIMENSION) {
                image = resize(image);
            }

            // 转换为 JPEG 以获得更小的文件大小
            ByteArrayOutputStream baos = new ByteArrayOutputStream();
            ImageIO.write(image, "jpg", baos);

            return baos.toByteArray();
        } catch (IOException e) {
            throw new ImageProcessingException("Failed to optimize image", e);
        }
    }

    /**
     * 处理前验证图像
     */
    public ValidationResult validate(byte[] imageData, String mimeType) {
        List<String> errors = new ArrayList<>();

        // 检查 MIME 类型
        if (!SUPPORTED_TYPES.contains(mimeType)) {
            errors.add("不支持的图像类型: " + mimeType);
        }

        // 检查大小
        if (imageData.length > 20 * 1024 * 1024) { // 20MB
            errors.add("图像超过最大大小 20MB");
        }

        // 尝试读取图像
        try {
            BufferedImage image = ImageIO.read(new ByteArrayInputStream(imageData));
            if (image == null) {
                errors.add("无法读取图像数据");
            } else if (image.getWidth() < 10 || image.getHeight() < 10) {
                errors.add("图像太小无法分析");
            }
        } catch (IOException e) {
            errors.add("无效的图像数据: " + e.getMessage());
        }

        return new ValidationResult(errors.isEmpty(), errors);
    }

    /**
     * 提取图像元数据
     */
    public ImageMetadata extractMetadata(byte[] imageData) {
        try {
            BufferedImage image = ImageIO.read(new ByteArrayInputStream(imageData));
            return new ImageMetadata(
                image.getWidth(),
                image.getHeight(),
                imageData.length,
                detectColorSpace(image),
                hasTransparency(image)
            );
        } catch (IOException e) {
            throw new ImageProcessingException("Failed to extract metadata", e);
        }
    }

    private BufferedImage resize(BufferedImage original) {
        double scale = Math.min(
            (double) MAX_DIMENSION / original.getWidth(),
            (double) MAX_DIMENSION / original.getHeight()
        );

        int newWidth = (int) (original.getWidth() * scale);
        int newHeight = (int) (original.getHeight() * scale);

        BufferedImage resized = new BufferedImage(newWidth, newHeight, BufferedImage.TYPE_INT_RGB);
        Graphics2D g = resized.createGraphics();
        g.setRenderingHint(RenderingHints.KEY_INTERPOLATION, RenderingHints.VALUE_INTERPOLATION_BILINEAR);
        g.drawImage(original, 0, 0, newWidth, newHeight, null);
        g.dispose();

        return resized;
    }

    public record ValidationResult(boolean valid, List<String> errors) {}
    public record ImageMetadata(int width, int height, long sizeBytes, String colorSpace, boolean hasTransparency) {}
}
```

### 5.3 视觉 API 的 REST 控制器

```java
@RestController
@RequestMapping("/api/v1/vision")
public class VisionController {

    private final VisionService visionService;
    private final ImageProcessingService imageProcessor;
    private final DocumentClassificationService docClassifier;
    private final InvoiceExtractionService invoiceExtractor;

    /**
     * 通用图像分析
     */
    @PostMapping("/analyze")
    public ResponseEntity<AnalysisResponse> analyzeImage(
            @RequestParam("image") MultipartFile image,
            @RequestParam("prompt") String prompt) throws IOException {

        // 验证
        var validation = imageProcessor.validate(image.getBytes(), image.getContentType());
        if (!validation.valid()) {
            return ResponseEntity.badRequest()
                .body(new AnalysisResponse(null, validation.errors()));
        }

        String result = visionService.analyzeImage(
            image.getBytes(),
            image.getContentType(),
            prompt
        );

        return ResponseEntity.ok(new AnalysisResponse(result, List.of()));
    }

    /**
     * 文档提取端点
     */
    @PostMapping("/extract-document")
    public ResponseEntity<?> extractDocument(
            @RequestParam("document") MultipartFile document,
            @RequestParam(value = "type", required = false) String documentType) throws IOException {

        byte[] docBytes = document.getBytes();
        String mimeType = document.getContentType();

        // 如果未提供类型，自动分类
        if (documentType == null) {
            var classification = docClassifier.classify(docBytes, mimeType);
            documentType = classification.documentType();
        }

        // 路由到适当的提取器
        return switch (documentType) {
            case "invoice" -> ResponseEntity.ok(
                invoiceExtractor.extractInvoice(docBytes, mimeType)
            );
            // 添加其他文档类型...
            default -> ResponseEntity.badRequest()
                .body("不支持的文档类型: " + documentType);
        };
    }

    /**
     * 比较多个图像
     */
    @PostMapping("/compare")
    public ResponseEntity<String> compareImages(
            @RequestParam("images") List<MultipartFile> images,
            @RequestParam("criteria") String criteria) throws IOException {

        List<VisionService.ImageInput> inputs = new ArrayList<>();
        for (MultipartFile img : images) {
            inputs.add(new VisionService.ImageInput(
                img.getContentType(),
                img.getBytes(),
                img.getOriginalFilename()
            ));
        }

        String prompt = String.format("""
            比较 %d 张基于以下标准的图像：%s

            提供详细的比较和排序。
            """, images.size(), criteria);

        String result = visionService.analyzeMultipleImages(inputs, prompt);
        return ResponseEntity.ok(result);
    }

    public record AnalysisResponse(String analysis, List<String> errors) {}
}
```

---

## 6. 模型特定优化

### 6.1 GPT-4V/GPT-4o 优化

```java
public class GPT4VPromptOptimizer {

    /**
     * GPT-4V 在以下方面表现最佳：
     * - Markdown 结构
     * - 清晰的节标题
     * - 编号的步骤
     * - 明确的输出格式规范
     */
    public String optimizeForGPT4V(String basePrompt) {
        return """
            ### 上下文
            您正在使用以下任务分析图像。

            ### 任务
            %s

            ### 说明
            1. 首先，描述您在图像中观察到的内容
            2. 然后，根据任务提供您的分析
            3. 最后，给出可行的建议

            ### 输出格式
            使用清晰的标题构建您的响应：
            - **观察**：您看到的内容
            - **分析**：您的评估
            - **建议**：建议的行动
            """.formatted(basePrompt);
    }

    /**
     * 为了最佳图像理解，GPT-4V 受益于：
     */
    public static final String GPT4V_BEST_PRACTICES = """
        1. 对复杂图像使用"高"细节模式
        2. 指定要关注的精确区域
        3. 请求逐步视觉分析
        4. 对不确定的观察询问置信度
        """;
}
```

### 6.2 Claude 视觉优化

```java
public class ClaudeVisionOptimizer {

    /**
     * Claude 擅长：
     * - XML 标签结构
     * - 详细的角色定义
     * - 明确的输出架构
     * - 思考过程阐述
     */
    public String optimizeForClaude(String basePrompt) {
        return """
            <role>
            您是具有细致关注力的专家视觉分析师。
            </role>

            <task>
            %s
            </task>

            <thinking_process>
            在提供您的分析之前：
            1. 仔细观察图像中的所有元素
            2. 考虑多种解释
            3. 验证您的观察
            4. 构建您的响应
            </thinking_process>

            <output_format>
            <observations>
            [您在图像中看到的内容]
            </observations>

            <analysis>
            [您的详细分析]
            </analysis>

            <recommendations>
            [可行的建议]
            </recommendations>
            </output_format>
            """.formatted(basePrompt);
    }

    /**
     * Claude 视觉特定最佳实践
     */
    public static final String CLAUDE_BEST_PRACTICES = """
        1. Claude 擅长文档 OCR - 对文本丰富的图像使用
        2. 询问区域时提供明确坐标
        3. Claude 可以原生处理 PDF - 优先使用 PDF 而非图像
        4. 使用 XML 标签进行结构化提取
        5. Claude 保守 - 明确询问不确定的答案
        """;
}
```

### 6.3 Gemini 2.0 优化

```java
public class GeminiVisionOptimizer {

    /**
     * Gemini 擅长：
     * - 跨模态推理
     * - 长视频分析
     * - 科学/技术图像
     * - 多步视觉推理
     */
    public String optimizeForGemini(String basePrompt) {
        return """
            您正在分析视觉内容。请遵循此过程：

            ## 步骤 1: 视觉清单
            列出您可以在图像中识别的所有不同元素。

            ## 步骤 2: 上下文分析
            基于这些元素，确定：
            - 这是什么场景/上下文？
            - 此视觉的目的是什么？

            ## 步骤 3: 任务执行
            %s

            ## 步骤 4: 验证
            检查您的分析：
            - 观察的准确性
            - 逻辑一致性
            - 完整性

            完成所有步骤后提供您的最终响应。
            """.formatted(basePrompt);
    }

    /**
     * Gemini 特定功能
     */
    public static final String GEMINI_CAPABILITIES = """
        1. 原生视频理解（最多 1 小时）
        2. 原生音频处理
        3. 最适合技术/科学图表
        4. 卓越的图表数据提取
        5. 多个图像之间的交叉引用
        6. 200 万 token 上下文用于大量多模态输入
        """;
}
```

---

## 7. 视频理解

### 7.1 基于帧的视频分析（GPT-4V/Claude）

```java
@Service
public class VideoAnalysisService {

    private final ChatClient chatClient;
    private final VideoFrameExtractor frameExtractor;

    /**
     * 通过提取关键帧分析视频
     */
    public VideoAnalysis analyzeVideo(
            byte[] videoData,
            String mimeType,
            VideoAnalysisConfig config) {

        // 按指定间隔提取帧
        List<Frame> frames = frameExtractor.extractFrames(
            videoData,
            config.frameInterval(),
            config.maxFrames()
        );

        // 批量分析帧
        List<FrameAnalysis> frameAnalyses = new ArrayList<>();
        for (int i = 0; i < frames.size(); i += config.batchSize()) {
            List<Frame> batch = frames.subList(i, Math.min(i + config.batchSize(), frames.size()));
            frameAnalyses.addAll(analyzeBatch(batch));
        }

        // 综合整体分析
        String synthesis = synthesizeVideoAnalysis(frameAnalyses, config.analysisGoal());

        return new VideoAnalysis(frameAnalyses, synthesis);
    }

    private List<FrameAnalysis> analyzeBatch(List<Frame> frames) {
        // 构建多图像提示
        StringBuilder prompt = new StringBuilder("""
            <task>
            按顺序分析这些视频帧。
            对于每帧，描述：
            1. 正在发生什么
            2. 关键对象/人员
            3. 与前一帧的任何变化
            </task>

            <frames>
            """);

        List<Media> mediaList = new ArrayList<>();
        for (int i = 0; i < frames.size(); i++) {
            Frame frame = frames.get(i);
            prompt.append(String.format("帧 %d（时间戳：%s）：\n", i + 1, frame.timestamp()));
            mediaList.add(new Media("image/jpeg", frame.data()));
        }

        prompt.append("</frames>");

        UserMessage.Builder messageBuilder = UserMessage.builder().text(prompt.toString());
        mediaList.forEach(messageBuilder::media);

        String response = chatClient.prompt()
            .user(messageBuilder.build())
            .call()
            .content();

        return parseFrameAnalyses(response, frames);
    }

    private String synthesizeVideoAnalysis(List<FrameAnalysis> analyses, String goal) {
        String analysisJson = toJson(analyses);

        return chatClient.prompt()
            .user(u -> u.text("""
                <task>
                将这些逐帧分析综合为连贯的视频摘要。
                </task>

                <analysis_goal>
                {goal}
                </analysis_goal>

                <frame_analyses>
                {analyses}
                </frame_analyses>

                <output_format>
                ## 视频摘要
                [发生内容的整体描述]

                ## 时间线
                - 0:00-0:30: [描述]
                - 0:30-1:00: [描述]
                ...

                ## 关键时刻
                1. [重要事件 + 时间戳]

                ## 分析
                [回答分析目标]

                ## 建议
                [如果适用]
                </output_format>
                """)
                .param("goal", goal)
                .param("analyses", analysisJson))
            .call()
            .content();
    }

    public record VideoAnalysisConfig(
        Duration frameInterval,
        int maxFrames,
        int batchSize,
        String analysisGoal
    ) {}

    public record Frame(byte[] data, Duration timestamp) {}
    public record FrameAnalysis(Duration timestamp, String description, List<String> objects, String changes) {}
    public record VideoAnalysis(List<FrameAnalysis> frames, String synthesis) {}
}
```

### 7.2 原生视频分析（Gemini）

```java
@Service
public class GeminiVideoService {

    private final VertexAiGeminiChatModel geminiModel;

    /**
     * Gemini 可以原生处理视频（最多 1 小时）
     */
    public String analyzeVideoNative(byte[] videoData, String prompt) {
        UserMessage message = new UserMessage(
            prompt,
            new Media("video/mp4", videoData)
        );

        return ChatClient.create(geminiModel)
            .prompt()
            .user(message)
            .call()
            .content();
    }

    /**
     * Gemini 视频分析提示
     */
    public String getVideoAnalysisPrompt(VideoAnalysisType type) {
        return switch (type) {
            case CONTENT_MODERATION -> """
                <task>
                审查此视频是否存在内容政策违规。
                </task>

                <check_for>
                - 暴力或有害内容
                - 不当语言
                - 版权侵犯（音乐、标志）
                - 个人信息暴露
                </check_for>

                <output_format>
                {
                  "overallSafe": boolean,
                  "violations": [
                    {
                      "type": "string",
                      "timestamp": "mm:ss",
                      "severity": "low/medium/high",
                      "description": "string"
                    }
                  ],
                  "recommendations": ["string"]
                }
                </output_format>
                """;

            case SUMMARIZATION -> """
                <task>
                提供此视频的全面摘要。
                </task>

                <include>
                - 涵盖的主要主题
                - 关键要点
                - 重要视觉/演示
                - 演讲者信息（如适用）
                - 行动项目或要点
                </include>

                <output_format>
                ## 视频摘要
                **时长**：[长度]
                **类型**：[教程/演示/采访等]

                ## 主要主题
                1. [主题] - [简短描述]

                ## 关键要点
                - [要点 1]
                - [要点 2]

                ## 时间线
                [mm:ss] - [发生什么]

                ## 推荐对象
                [谁会从此视频中受益]
                </output_format>
                """;

            case TUTORIAL_EXTRACTION -> """
                <task>
                从此教程视频中提取逐步说明。
                </task>

                <output_format>
                ## 教程：[标题]

                ### 先决条件
                - [要求 1]

                ### 步骤
                1. **[步骤标题]**（时间戳：mm:ss）
                   - 描述：[做什么]
                   - 工具/材料：[如有]
                   - 提示：[有用的提示]

                2. **[步骤标题]**（时间戳：mm:ss）
                   ...

                ### 常见错误
                - [错误 + 如何避免]

                ### 最终结果
                [预期成果的描述]
                </output_format>
                """;
        };
    }

    public enum VideoAnalysisType {
        CONTENT_MODERATION,
        SUMMARIZATION,
        TUTORIAL_EXTRACTION
    }
}
```

---

## 8. 多模态 RAG

### 8.1 图像-文本 RAG 架构

```java
@Service
public class MultimodalRAGService {

    private final ChatClient chatClient;
    private final VectorStore vectorStore;
    private final EmbeddingModel embeddingModel;
    private final VisionService visionService;

    /**
     * 使用生成的描述索引图像
     */
    public void indexImage(String imageId, byte[] imageData, String mimeType, Map<String, String> metadata) {
        // 生成详细描述
        String description = visionService.analyzeImage(
            imageData,
            mimeType,
            """
            提供此图像的详细、可搜索描述，包括：
            - 主要主题和对象
            - 颜色、纹理和视觉风格
            - 场景和上下文
            - 任何可见的文本
            - 氛围和情绪
            使用具体、具体的术语，以便人们可能搜索。
            """
        );

        // 从描述创建嵌入
        float[] embedding = embeddingModel.embed(description);

        // 存储元数据
        Document doc = Document.builder()
            .id(imageId)
            .content(description)
            .embedding(embedding)
            .metadata(Map.of(
                "type", "image",
                "mimeType", mimeType,
                "originalMetadata", metadata.toString()
            ))
            .build();

        vectorStore.add(List.of(doc));
    }

    /**
     * 文本查询，检索相关图像
     */
    public List<ImageSearchResult> searchImages(String query, int topK) {
        // 通过文本搜索
        List<Document> results = vectorStore.similaritySearch(
            SearchRequest.query(query).withTopK(topK)
        );

        return results.stream()
            .filter(doc -> "image".equals(doc.getMetadata().get("type")))
            .map(doc -> new ImageSearchResult(
                doc.getId(),
                doc.getContent(),
                doc.getMetadata(),
                calculateRelevance(query, doc)
            ))
            .collect(Collectors.toList());
    }

    /**
     * 图像查询，检索相似图像
     */
    public List<ImageSearchResult> searchSimilarImages(byte[] queryImage, String mimeType, int topK) {
        // 生成查询图像的描述
        String queryDescription = visionService.analyzeImage(
            queryImage,
            mimeType,
            "描述此图像以进行相似性搜索。"
        );

        return searchImages(queryDescription, topK);
    }

    /**
     * 多模态问答：使用检索的图像回答问题
     */
    public String answerWithImages(String question, int retrievalCount) {
        // 检索相关图像
        List<ImageSearchResult> images = searchImages(question, retrievalCount);

        // 从检索的图像构建上下文
        String context = images.stream()
            .map(img -> String.format("图像 %s：%s", img.id(), img.description()))
            .collect(Collectors.joining("\n\n"));

        // 使用上下文回答
        return chatClient.prompt()
            .user(u -> u.text("""
                <task>
                使用检索到的图像信息回答问题。
                </task>

                <question>
                {question}
                </question>

                <retrieved_image_descriptions>
                {context}
                </retrieved_image_descriptions>

                <instructions>
                - 基于图像描述回答
                - 引用支持您答案的图像
                - 如果图像不包含相关信息，请说明
                </instructions>
                """)
                .param("question", question)
                .param("context", context))
            .call()
            .content();
    }

    public record ImageSearchResult(
        String id,
        String description,
        Map<String, Object> metadata,
        double relevance
    ) {}
}
```

### 8.2 文档多模态 RAG

```java
@Service
public class DocumentMultimodalRAG {

    private final ChatClient chatClient;
    private final VectorStore vectorStore;
    private final VisionService visionService;

    /**
     * 索引 PDF，同时考虑文本和视觉理解
     */
    public void indexPDF(String documentId, byte[] pdfData) {
        // 提取文本块
        List<String> textChunks = extractTextChunks(pdfData);

        // 提取并描述图像/图表/表格
        List<VisualElement> visualElements = extractVisualElements(pdfData);

        // 索引文本块
        for (int i = 0; i < textChunks.size(); i++) {
            indexTextChunk(documentId, i, textChunks.get(i));
        }

        // 索引视觉元素
        for (VisualElement visual : visualElements) {
            indexVisualElement(documentId, visual);
        }
    }

    private void indexVisualElement(String documentId, VisualElement visual) {
        // 生成丰富描述
        String description = visionService.analyzeImage(
            visual.imageData(),
            "image/png",
            String.format("""
                这是来自文档的 %s。详细描述它：
                - 它传达什么信息？
                - 关键数据点或元素是什么？
                - 它如何与典型文档内容相关？

                上下文：这出现在文档的第 %d 页。
                """, visual.type(), visual.pageNumber())
        );

        Document doc = Document.builder()
            .id(documentId + "_visual_" + visual.id())
            .content(description)
            .metadata(Map.of(
                "documentId", documentId,
                "type", "visual_" + visual.type(),
                "pageNumber", visual.pageNumber(),
                "elementType", visual.type()
            ))
            .build();

        vectorStore.add(List.of(doc));
    }

    /**
     * 考虑文本和视觉内容的查询
     */
    public RAGResponse queryDocument(String documentId, String question) {
        // 搜索所有内容类型
        List<Document> textResults = searchByType(documentId, question, "text");
        List<Document> visualResults = searchByType(documentId, question, "visual");

        // 构建全面上下文
        String textContext = formatTextContext(textResults);
        String visualContext = formatVisualContext(visualResults);

        // 生成答案
        String answer = chatClient.prompt()
            .user(u -> u.text("""
                <task>
                使用此文档中的信息回答问题。
                </task>

                <question>
                {question}
                </question>

                <text_excerpts>
                {textContext}
                </text_excerpts>

                <visual_descriptions>
                {visualContext}
                </visual_descriptions>

                <instructions>
                1. 考虑文本和视觉信息
                2. 引用特定部分或图表
                3. 如果答案涉及图表/表格中的数据，请引用数值
                4. 如果信息不完整，表示置信度
                </instructions>
                """)
                .param("question", question)
                .param("textContext", textContext)
                .param("visualContext", visualContext))
            .call()
            .content();

        return new RAGResponse(answer, textResults, visualResults);
    }

    public record VisualElement(
        String id,
        String type,  // chart, table, figure, diagram
        int pageNumber,
        byte[] imageData
    ) {}

    public record RAGResponse(
        String answer,
        List<Document> textSources,
        List<Document> visualSources
    ) {}
}
```

---

## 9. 安全和隐私考虑

### 9.1 图像净化

```java
@Service
public class ImageSecurityService {

    /**
     * 处理前检查图像中的敏感内容
     */
    public SecurityCheck checkImage(byte[] imageData, String mimeType) {
        List<String> concerns = new ArrayList<>();

        // 检查隐写术或隐藏数据
        if (containsHiddenData(imageData)) {
            concerns.add("图像可能包含隐藏数据");
        }

        // 检查 EXIF 中的敏感元数据
        ExifData exif = extractExif(imageData);
        if (exif.hasGpsCoordinates()) {
            concerns.add("图像包含 GPS 坐标");
        }
        if (exif.hasCameraSerialNumber()) {
            concerns.add("图像包含设备标识符");
        }

        return new SecurityCheck(
            concerns.isEmpty(),
            concerns,
            exif
        );
    }

    /**
     * 发送到 API 前净化图像
     */
    public byte[] sanitizeImage(byte[] imageData, SanitizationConfig config) {
        BufferedImage image = readImage(imageData);

        // 移除 EXIF 元数据
        image = stripExifData(image);

        // 可选：模糊人脸
        if (config.blurFaces()) {
            image = detectAndBlurFaces(image);
        }

        // 可选：涂抹文本
        if (config.redactText()) {
            image = detectAndRedactText(image);
        }

        // 可选：涂抹特定区域
        for (Region region : config.redactRegions()) {
            image = redactRegion(image, region);
        }

        return toBytes(image, "image/jpeg");
    }

    /**
     * 检查 API 响应中的数据泄露
     */
    public boolean checkResponseForLeakage(String response, List<String> sensitivePatterns) {
        for (String pattern : sensitivePatterns) {
            if (Pattern.compile(pattern).matcher(response).find()) {
                return true;
            }
        }
        return false;
    }

    public record SecurityCheck(
        boolean safe,
        List<String> concerns,
        ExifData exifData
    ) {}

    public record SanitizationConfig(
        boolean blurFaces,
        boolean redactText,
        List<Region> redactRegions
    ) {}

    public record Region(int x, int y, int width, int height) {}
}
```

### 9.2 内容审核

```java
@Service
public class VisionContentModerationService {

    private final ChatClient chatClient;

    private static final String MODERATION_PROMPT = """
        <role>
        您是内容审核系统。分析此图像是否存在政策违规。
        </role>

        <policies>
        以下内容不被允许：
        1. 暴力或图形内容
        2. 成人/性内容
        3. 仇恨符号或歧视性图像
        4. 个人信息（身份证、信用卡等）
        5. 危险活动或自残
        6. 欺骗性或误导性内容
        7. 受版权保护的材料（标志、角色）
        </policies>

        <output_format>
        返回 JSON：
        {
          "approved": boolean,
          "violations": [
            {
              "policy": "string",
              "severity": "low/medium/high/critical",
              "description": "string",
              "region": "图像中位置的描述"
            }
          ],
          "confidence": 0.0-1.0,
          "recommendations": ["string"]
        }
        </output_format>
        """;

    public ModerationResult moderate(byte[] imageData, String mimeType) {
        UserMessage message = new UserMessage(
            MODERATION_PROMPT,
            new Media(mimeType, imageData)
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .entity(ModerationResult.class);
    }

    public record ModerationResult(
        boolean approved,
        List<Violation> violations,
        double confidence,
        List<String> recommendations
    ) {}

    public record Violation(
        String policy,
        String severity,
        String description,
        String region
    ) {}
}
```

---

## 10. 性能优化

### 10.1 缓存策略

```java
@Service
public class VisionCacheService {

    private final Cache<String, CachedAnalysis> analysisCache;

    public VisionCacheService() {
        this.analysisCache = Caffeine.newBuilder()
            .maximumSize(1000)
            .expireAfterWrite(Duration.ofHours(24))
            .build();
    }

    /**
     * 基于图像哈希 + 提示哈希的缓存键
     */
    public String generateCacheKey(byte[] imageData, String prompt) {
        String imageHash = DigestUtils.sha256Hex(imageData);
        String promptHash = DigestUtils.sha256Hex(prompt);
        return imageHash + "_" + promptHash;
    }

    /**
     * 获取或计算分析
     */
    public String getOrAnalyze(
            byte[] imageData,
            String prompt,
            Supplier<String> analyzer) {

        String key = generateCacheKey(imageData, prompt);

        CachedAnalysis cached = analysisCache.getIfPresent(key);
        if (cached != null) {
            return cached.analysis();
        }

        String result = analyzer.get();
        analysisCache.put(key, new CachedAnalysis(result, Instant.now()));

        return result;
    }

    public record CachedAnalysis(String analysis, Instant timestamp) {}
}
```

### 10.2 批量处理

```java
@Service
public class BatchVisionService {

    private final ChatClient chatClient;
    private final ExecutorService executor;

    public BatchVisionService(ChatClient chatClient) {
        this.chatClient = chatClient;
        this.executor = Executors.newFixedThreadPool(4);
    }

    /**
     * 并行处理多个图像
     */
    public List<BatchResult> processBatch(
            List<ImageTask> tasks,
            int concurrency) {

        Semaphore semaphore = new Semaphore(concurrency);
        List<CompletableFuture<BatchResult>> futures = new ArrayList<>();

        for (ImageTask task : tasks) {
            CompletableFuture<BatchResult> future = CompletableFuture.supplyAsync(() -> {
                try {
                    semaphore.acquire();
                    String result = processImage(task);
                    return new BatchResult(task.id(), result, null);
                } catch (Exception e) {
                    return new BatchResult(task.id(), null, e.getMessage());
                } finally {
                    semaphore.release();
                }
            }, executor);

            futures.add(future);
        }

        return futures.stream()
            .map(CompletableFuture::join)
            .collect(Collectors.toList());
    }

    private String processImage(ImageTask task) {
        UserMessage message = new UserMessage(
            task.prompt(),
            new Media(task.mimeType(), task.imageData())
        );

        return chatClient.prompt()
            .user(message)
            .call()
            .content();
    }

    public record ImageTask(
        String id,
        byte[] imageData,
        String mimeType,
        String prompt
    ) {}

    public record BatchResult(
        String id,
        String result,
        String error
    ) {}
}
```

---

## 11. 常见错误和解决方案

| 错误 | 问题 | 解决方案 |
|---------|---------|----------|
| **模糊的提示** | "描述此图像" | 精确指定要关注的方面和输出格式 |
| **没有输出架构** | 无法解析的响应 | 始终指定 JSON/结构化输出 |
| **忽略图像质量** | 差的提取结果 | 预处理：调整大小、增强对比度、修复方向 |
| **图像过载** | 令牌限制、处理慢 | 批量处理、使用适当分辨率 |
| **没有验证** | "我看不到"响应未处理 | 检查错误模式、实施回退 |
| **敏感数据暴露** | PII 在图像中发送到 API | 净化图像、涂抹敏感区域 |
| **单一模型方法** | 次优结果 | 使用模型特定提示、为每个任务选择最佳模型 |
| **没有缓存** | 重复的 API 调用 | 按图像+提示哈希缓存 |

---

## 12. 快速参考

### 按用例的提示模板

```java
public class VisionPromptTemplates {

    public static final String OCR = """
        提取此图像中的所有文本逐字不差。
        保留格式、换行和结构。
        作为纯文本返回，不返回 JSON。
        """;

    public static final String CLASSIFICATION = """
        将此图像精确分类为以下类别之一：
        {categories}

        返回 JSON：{"category": "...", "confidence": 0.0-1.0, "reasoning": "..."}
        """;

    public static final String DATA_EXTRACTION = """
        从此文档中提取以下字段：
        {fields}

        返回包含字段名称作为键的 JSON。缺失字段使用 null。
        """;

    public static final String COMPARISON = """
        比较这些图像并识别：
        1. 相似之处
        2. 差异
        3. 对于以下标准的最佳选择：{criteria}

        提供结构化分析。
        """;

    public static final String ACCESSIBILITY = """
        为此图像生成适合屏幕阅读器的可访问性描述。
        包括：主要主题、重要细节、文本内容、有意义的颜色。
        保持 200 字以内。
        """;
}
```

---

## 参考资料

### 文档

- [OpenAI GPT-4V 视觉文档](https://platform.openai.com/docs/guides/vision)
- [Anthropic Claude 视觉指南](https://docs.anthropic.com/claude/docs/vision)
- [Google Gemini 多模态](https://ai.google.dev/docs/multimodal_concepts)
- [Spring AI 视觉支持](https://docs.spring.io/spring-ai/reference/)

### 研究论文

1. Alayrac 等 (2022)："Flamingo: a Visual Language Model for Few-Shot Learning"
2. Liu 等 (2024)："Visual Instruction Tuning" (LLaVA)
3. OpenAI (2024)："GPT-4V(ision) System Card"
4. Google (2024)："Gemini: A Family of Highly Capable Multimodal Models"
5. Anthropic (2024)："Claude 3 Model Card"

---

**上一页**：[3.1 高级技术](./07-advanced-techniques.mdx) ←
**下一页**：[3.3 代理编排](./09-agent-orchestration.mdx) →